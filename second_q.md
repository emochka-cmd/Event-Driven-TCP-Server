НЕБЛОКИРУЮЩИЙ - НЕ ЖДЕТ КАЖДОГО КЛИЕНТА

нужно:

- реализовать epool wait
- продумать
-EPOLLRDHUP | EPOLLHUP | EPOLLERR для user

- вместо accept/recv/send должно идти:
    пока сервер жив:
        узнать, какие сокеты готовы
        обработать только их

- механизм опроса(прочитать про доступные, предпочтительно epool)

- сделать ориентированом на события


выполнено: 
- перевести listen + accept в non-blocking. Меняеться то, что accept сразу передает, recv/send/ либо возращает, либо EAGAIN.  
- сделать сервер неблокирующим